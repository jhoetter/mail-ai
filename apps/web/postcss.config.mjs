// Tailwind v4 PostCSS plugin. v4 is CSS-first: theme + content sources
// are declared inside globals.css via @theme / @source, so this file
// only needs the plugin entry.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
